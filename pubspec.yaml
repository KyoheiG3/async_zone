name: workspace
description: "A new Flutter package project."

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

workspace:
  - packages/async_zone
  - packages/error_boundary
  - packages/hooks_async_zone
