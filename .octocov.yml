# generated by octocov init
coverage:
  if: true
  paths:
    - packages/async_zone/coverage/lcov.info
    - packages/async_error_boundary/coverage/lcov.info
    - packages/hooks_async_zone/coverage/lcov.info
    - packages/async_transition_boundary/coverage/lcov.info
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
