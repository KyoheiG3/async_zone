name: async_zone_sample
description: "Sample app demonstrating async_zone, hooks_async_zone, and async_error_boundary."
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  async_zone:
  hooks_async_zone:
  async_transition_boundary:
  async_error_boundary:
  cupertino_icons: ^1.0.8
  flutter_hooks: ^0.21.3
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
